
# Employee Attendance (Frontend)

## Setup

1. Install dependencies:
   ```bash
   npm install
   ```
2. Run dev server:
   ```bash
   npm run dev
   ```
3. Open http://localhost:5173

This is a frontend-only React + Tailwind app. The API base URL is set to `http://localhost:5000/api` in `src/services/api.js` — update if needed.
